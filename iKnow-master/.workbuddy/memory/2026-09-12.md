# 2026-09-12

## 新增：场景化 AI 路由层（PRD §14.1/§14.2/§15.2）
用户诉求：根据不同场景（对话/统计/分析…）调用相应 AI。原项目 5 个 Agent 已就绪，但缺统一「场景→Agent」分发入口，调用方需自己知道该调哪个方法。

实现（仅改 AI 层，UI/页面零改动）：
- `services/ai/AIService.ets`：新增 `AIPayload`/`AIResult` 联合类型 + 接口方法 `dispatch(req)`。
- `services/ai/MockAIService.ets`：实现 `dispatch`，按 `req.scene` 分发到 plan/dialog/weeklyInsight/decide/askText/askImage/generate/update，复用 runWithRetry（超时/重试/兜底 + SchemaValidator）。
- `services/ai/BridgeConfig.ets`（新）：scene→模型映射（qwen-plus / qwen-vl-plus / qwen-turbo）+ 每场景超时；API Key 仅占位（硬规则 §10/§11，密钥不下发客户端）。
- `services/ai/GatewayAIService.ets`：实现 `dispatch` 选模型 + 构造 OpenAI 兼容请求体（占位，未启用回退 not_configured）。
- `services/AIService.ets`（垫片/门面）：加 `setGatewayEnabled`、`dispatch`、便捷 `callScene(scene, payload)`；开启网关时先试 Gateway，失败自动回退 Mock。

调用方式：`AIService.getInstance().callScene('qa_text', { question: '...' })`，返回 `AIResponse<AIResult>` 按 scene 区分结构。

冲突点：cast 走 `as Object as Promise<AIResponse<AIResult>>` 以兼容 ArkTS 严格 as 规则；未改任何页面/AppStore 现有调用。

## 新增：登录 + 3 种学伴人格 + 首页单一规划入口 + AI 规划聊天页（5 项需求）
用户第 3 条需求，全部实现且此前 DevEco Studio 构建 SUCCEESSFUL：
- 登录：`services/AuthService.ets`（新，本地账号 djb2 散列，无后端/无 API Key，符合硬规则 §10）；`common/Constants.ets` 加 `AuthKey`；`EntryAbility` 登录门禁（init 后按 `AuthService.isLoggedIn()` 决定进 Login/Index）；`pages/Login.ets`（新）；`main_pages.json` 首条加 `pages/Login`。
- 用户名全局生效：`AppStore.applyLoginUser()` 写 `user.nickname` + AppStorage `USER_NICKNAME`，首页/我的响应。
- 3 种学伴：`pages/Mine.ets` 改为 3 张人格卡（STRICT 严格教练型 / GENTLE 温和陪伴型 / QUIET 极简安静型；第 4 种 ADAPTIVE 智能适配在 Settings）。
- 首页单一入口：`pages/Home.ets` 去掉「每点一个框进聊天」，新增「AI 智能规划」总按钮 → `pages/TaskPlanning`；其余卡片保持原路由。
- AI 规划聊天页：`pages/TaskPlanning.ets`（重写）—— 3 轮对话（目标/细节/可用时间）→ `planByScene`/`analyzePlan` 出计划；计划表单置于对话下方，每条可点开编辑（弹窗改标题/科目/时长±5min/删除），每条前有圆圈点击确认变实心；底部「✓ 进入 X 分钟专注学习」+「确认并开始专注」→ `setCurrentPlan` 后 `router.replaceUrl('pages/Focus')`。文案均来自 AIService（硬规则 §1，不写死页面）。
- AI 接入：`AIService` 门面加 `planningIntro/analyzePlan/planByScene/dialogByScene/nextPlanningQuestion`；`MockAIService` dispatch 已含 `task_plan`/`task_dialog`/`planning` 场景，全部方法齐备。

## 构建环境说明（重要）
CLI 构建可用，但 hvigorw 不在项目根，而在 DevEco 安装目录：
`"D:/soft/env/DevEco Studio/tools/node/node.exe" "D:/soft/env/DevEco Studio/tools/hvigor/bin/hvigorw.js" --mode module -p module=entry@default -p product=default -p requiredDeviceType=phone assembleHap --analyze=normal --parallel --incremental --daemon`
（项目根无 hvigorw.js / node_modules，所以裸 `node hvigorw.js` 会失败。）
本会话对 5 项改动做静态集成核查全部通过；随后跑**干净重建（clean + assembleHap）**，结果 `BUILD SUCCESSFUL in 28 s`，**零 ERROR**。

关于 `@Entry export` 预览 WARN（"not a recommended way to export struct with '@Entry'..."）：
- 这是**项目级既有问题**：pages/ 下共 14 个文件带 `export struct`（AIChat/FocusBreak/Device/Focus/Privacy/FocusProfile/Mine/AICompanion/Settings/StudyReport/Dashboard/Home/Review/Placeholder），非本次改动引入。
- 本次新建/重写的 `Login.ets`、`TaskPlanning.ets` 已改为非 export 的 `struct`，其 @Entry export 警告已消除（干净重建中不再出现 Login.ets:7 / TaskPlanning.ets:22 的该警告；仅剩 replaceUrl/back 废弃 API 警告，非阻断）。
- 其余 14 个文件为该废弃写法，属预览模式 WARN、不阻断构建；如需清爽可一次性改为 `struct`（与项目「不改动无关文件」规则冲突，需用户确认再做）。
- **补充认知**：pages/ 下被 Index 以组件形式引用的页面（Dashboard/Review/Mine/Home/TaskPlanning…）**必须 `export struct`**，所以 @Entry+export 的预览警告是这套「页面即 Tab 子组件」架构的固有产物，不能简单去掉 export（去掉会导致 Index 无法 import）。

## 大改：首页=工作台、工作台槽位=AI规划、复盘页平滑曲线+日历切换单日
用户诉求（本次"大改"）：①首页换成工作台；②工作台顶部不显示"我的工作台"，改为按时间问候+登录用户名；③原工作台（第 2 个页面）换成"AI 规划"；④复盘-近30天表现折线改平滑曲线；⑤复盘右上角日历可切换"长期学习总结/某天学习总结"。
经 AskUserQuestion 确认：Tab 布局=工作台/AI规划/复盘/我的；工作台沿用现有内容仅换顶部；单日总结按所选日期聚合。

变更：
- `pages/Index.ets`：Tab0 `Dashboard`（标签"工作台"）、Tab1 `TaskPlanning`（标签"AI 规划"）、Tab2 `Review`、Tab3 `Mine`；移除 Home import。
- `pages/TaskPlanning.ets`：恢复 `export`（Index 需 import），仍保留 @Entry 以兼容 Review 的 pushUrl 路由。
- `pages/Dashboard.ets`：新增 `@StorageProp(USER_NICKNAME)`，顶部标题改为 `${TimeUtils.greeting(now)}，${用户名}`（复用 TimeUtils.greeting，含 早上/中午/下午/晚上好）；主体内容不变。
- `services/Metrics.ets`：新增 `DaySummary` 接口 + `static daySummary(sessions, dayStart)`，按天过滤会话聚合 学习时长/专注率/任务完成/答疑会话数/最佳时段/当日曲线（§3）。
- `services/ai/MockAIService.ets` + `services/AIService.ets`：新增 `dayInsight` / `daySuggestions`（单日复盘 AI 文案，§1）。
- `pages/Review.ets`：整体重写。①`curveThrough()` 用二次贝塞尔过中点把折线改平滑曲线（填充+描边都平滑）；②右上角 📅 → `DatePickerDialog.show` 选日期 → `mode=1` 单日视图（复刻图3：状态卡+CircularProgress 圆环+FocusChart 曲线+4 指标+AI洞察+学习建议+返回长期）；③`✕` 返回长期。
- `store/AppStore.ets`：**修 bug** —— `initAppStorageDefaults` 把 USER_NICKNAME 写死为"刘灿"，自动登录（重启动）时问候语会显示默认名而非登录名；在 `doInit` 载入 user 后补 `AppStorage.setOrCreate(USER_NICKNAME, this.user.nickname)` 回填。

验证：DevEco hvigorw 干净重建 + 增量重建均 **BUILD SUCCESSFUL，零 ERROR**。剩余告警均为既有风格：`DatePickerDialog.show`/`router.pushUrl`/`replaceUrl`/`back` 废弃 + @Entry export 预览。

## 补充：用户头像支持 + 复盘日历弹层（用户"补充1"）
用户诉求：①（接上次可选1）复盘页日期选择做成"更明显的日历弹层"替代系统 `DatePickerDialog`；②支持改用户头像；③工作台右上角、AI规划聊天界面加入头像。
- 头像数据模型此前已就绪（`User.avatar`、`StorageKey.USER_AVATAR`、`initAppStorageDefaults`、`pushAggregates` 已写）。本次补：
- `store/AppStore.ets`：新增 `saveUserAvatar(avatar)` → 持久化 `user.avatar` + AppStorage `USER_AVATAR`。
- `pages/Mine.ets`：头像 `Text` 加 onClick 打开 `bindSheet` 底栏（`avatars` 16 个 emoji 网格），选中调 `saveUserAvatar`；`@StorageProp(USER_AVATAR)` 已读。
- `pages/Dashboard.ets`：顶部问候 Row 右侧加圆形头像（`@StorageProp(USER_AVATAR)`）。
- `pages/TaskPlanning.ets`：补 `import StorageKey` + `@StorageProp(USER_AVATAR)`，顶部栏右侧（AITag 前）加圆形头像。
- `pages/Review.ets`（补充1）：`pickDay()` 改为 `showCalendar=true`；新增 `calCells/isFuture/isSameDay/shiftMonth/confirmDay` + `@Builder calDayCell/calendarSheet`（7列 Grid 月历，上/下月切换、禁选未来、选中日=今日高亮、点击切 `mode=1` 单日视图）；根 Scroll 加 `bindSheet(showCalendar, ..., {height:'72%', showClose:true})`。

验证：增量 assembleHap **BUILD SUCCESSFUL**，仅既有 WARN（showToast/pushUrl 废弃 + @Entry export）。

## 接入真实大模型 API + 修复对话不进行 + 全局更名「考伴」
用户诉求：①接入 PRD 里的 API，让 AI 规划对话真正跑通；②对话正常进行 + 用户话里含规划信息时挖掘任务并给建议（学 xx 多久、什么时候学）；③全项目「学伴」→「考伴」。

### 接入（PRD §15.2）
- `services/ai/BridgeConfig.ets`：填入 PRD 的 BASE_URL / API_KEY，`useGateway=true`，新增 `MODEL_TEXT/MODEL_MULTIMODAL/MODEL_FAST`。
- `services/ai/GatewayAIService.ets`：**整体重写为真实 HTTP**——`@kit.NetworkKit` 的 `http.createHttp()` POST `{base}/chat/completions`；先带 `response_format:{type:'json_object'}`，失败则**去掉该参数重试一次**；`extractJson()` 抠最外层 `{}`（兼容 ```json 与前后废话）；输出过 `SchemaValidator`，任一步失败返回 `ok:false` → 门面自动回退 Mock。已开放 `task_plan`/`task_dialog`，其余场景保持 not_implemented 走 Mock。
- `entry/src/main/module.json5`：新增 `ohos.permission.INTERNET`（system_grant，无需动态申请）——**漏了它网络请求必失败**。
- `services/AIService.ets`：构造函数按 `BridgeConfig.useGateway` 启用网关。
- ⚠️ 安全取舍：Key 现在落在客户端 `BridgeConfig`（违反硬规则 §10）。仅本地联调可用；上线前把 Key 留空、BASE_URL 指向后端 BFF，其余零改动。

### 🐞 修掉「对话不进行」的真 bug
`SchemaValidator.for('task_dialog')` 之前**错用了 `planValidator`**（要求 `tasks[] + summary`），而 `PlanningDialogResult` 是 `{reply, done}` → 校验永远失败 → `MockAIService.dialog` 永远返回兜底文案，对话自然走不下去。已拆出独立 `dialogValidator`（校验 `reply:string` + `done:boolean`）。

### Prompt（新增 `services/ai/Prompts.ets`，对齐 §14.6，带版本号）
- `task_dialog@1.0.0`：自然对话（2~4 句、每次最多 1 问、禁列表/Markdown）+ 挖掘 5 项（学什么/做什么及题量/可用时间/何时学/优先项）；拿到「学什么 + 大概可用时间」就输出 `done:true + tasks`，并在 reply 里给**具体到时间数字**的排序理由、建议时段、单段时长、30~45 分钟休息 5~10 分钟。
- `task_plan@1.0.0`：单轮拆 2~5 条任务 + `summary` 时段/休息建议。
- 人格（STRICT/GENTLE/QUIET）映射到语气。

### 规划页改 AI 驱动（`TaskPlanning.ets`）
去掉固定 3 轮 `phase` 脚本：新增 `turns` / `showComposer` / `history`（[user,ai,…] 交替）/ `userTexts`。每次发言带完整历史调 `task_dialog`；`done && tasks.length>0` → 直接落计划；聊满 4 轮仍无计划 → 兜底 `planByScene`；新增 `planningHold()`（MockAIService+AIService 门面）做无回复承接语（§1：页面不写死 AI 文案）；计划下方加「💬 继续和 AI 考伴调整」回到输入态。

### 实测（curl + python 直连）
- 端点/Key 有效：HTTP 200，`response_format: json_object` 被接受，返回严格符合 Schema。
- 闲聊「今天好累啊」→ 共情回应、`done:false`、不硬塞计划。
- 含规划信息「复习高数级数，今晚两小时」→ 追问缺失项（挖掘行为）。
- 信息充分（七点开始/10 道题/两小时/先高数）→ `done:true`，拆出 3 条任务（高等数学 45+15min、概率论与数理统计 40min，合计 100min）并给安排建议。

### 更名
`学伴` → `考伴` 全量替换：「专注感知智能考伴」、「AI 考伴」、「考伴类型/人格」、「和考伴聊聊」；覆盖 ets 源码 9 个文件 + `string.json`(base/zh_CN) + `oh-package.json5` + README/PRD/需求文档/优化建议。`Product.COMPANION_NAME='小伴'` 保留。文件名 `iKnow——专注感知智能学伴 App 开发需求.md` 未改名（仅改内容）。

验证：DevEco hvigorw **BUILD SUCCESSFUL，零 ERROR**（WARN 137→129）；新增文件 GatewayAIService/Prompts/SchemaValidator/BridgeConfig **零 WARN**。

## 修 4 项反馈：头像改覆盖层 / 工作台删 AI 洞察 / 复盘曲线交互 / 复盘头部下拉
用户反馈：①换不了头像；②工作台删掉「AI 洞察」；③近30天曲线别默认画点，点按才出点、左右拖拽数值跟着变；④复盘去掉左上角 iKnow，右上角日历显示当天日期、点击下拉切换「长期/某天」复盘。

### ① 头像：改用自定义覆盖层（关键修复）
- 判断：原实现用 `bindSheet` + `$$`，在 **Tabs 子页面**里不可靠（点关闭后再点打不开 / 干脆不弹）。改为**自定义全屏覆盖层**。
- 新增 `components/AvatarPicker.ets`：`Stack` 遮罩(点空白关闭) + 底部面板；参数 `@Prop show` + `onClose` 回调（**不用 `@Link`/`bindSheet`**，父子单向最稳）；内含「从相册选择照片」(`PhotoViewPicker`+拷沙箱) 与 16 emoji 网格。
- `pages/Mine.ets`、`pages/Dashboard.ets` 均接入：**工作台右上角头像也可点开改**（以前只有「我的」能改，用户很可能点的就是工作台那个）。两个页面 `build()` 都改为 `Stack{ Scroll{} + AvatarPicker }`。

### ② 工作台删 AI 洞察
`pages/Dashboard.ets`：删掉「AI 洞察」卡片，连带移除 `aiInsight`/`hardestSubject` 状态与计算、`AIService` 与 `AITag` 引用（避免留下无用 import 告警）。

### ③ 复盘曲线：默认无点 + 点按/拖拽显值
`pages/Review.ets`：
- 删除原「顶点」循环 → **默认不画任何数据点**。
- 缓存几何参数（`geoL/geoStep/geoT/geoChartH/geoMaxV/geoN/geoW`），`Canvas.onTouch` 里按 `x` 反算最近下标 → `activeIndex`，重绘。
- 选中时画：垂直参考线 + 高亮点（白心绿环）；松手保留（方便读数）。
- 数值气泡用 **ArkUI 浮层**（`Stack` 内 `if(activeIndex>=0)` 的 `Column` + `.position({x,y})`），文案「M月D日 / X 分钟」，贴边自动收敛。`trend[i]` 单位=当日净专注**分钟**（0=29天前，29=今天）。

### ④ 复盘头部：删 iKnow + 日期 chip 下拉
- 去掉左上角「iKnow」；左侧只留标题。
- 右上角改为「📅 + 日期」chip（长期=今天、按天=所选日，含 ▾）。
- 点击 chip → **右上角下拉面板**（`Stack({alignContent:TopEnd})` + 遮罩 + `.margin({top:62,right:16})` 面板）：两个选项「📈长期复盘 / 📅按天复盘」，选「按天」在面板内展开紧凑月历选日；选日即切单日视图并收起面板。移除原 `bindSheet`/`calendarSheet`。

验证：DevEco hvigorw **clean + 增量 assembleHap 均 BUILD SUCCESSFUL，零 ERROR**，WARN 总数 137（与改动前一致，无新增类别）。

## 头像能力升级 + AI 规划页按截图重做（微信式对话）
用户诉求：①「修改头像」功能要真正可用；②AI 规划页严格对齐截图（微信式一来一回、两侧都有头像、标题居中）。

### 头像
- `components/UserAvatar.ets`（新）：统一渲染 emoji 与「相册照片」两种形态（`file://` 前缀 → `Image`，否则 `Text`）。**新建组件零 WARN**。参数：`avatar / diameter / radius / bg`。
- `pages/Mine.ets`：头像面板新增「🖼️ 从相册选择照片」——`photoAccessHelper.PhotoViewPicker`（系统选择器，**无需申请权限**）→ `fileIo` 拷贝到 `ctx.filesDir/avatar_<ts>.jpg` → `AppStore.saveUserAvatar('file://'+destPath)`；emoji 网格改用 `UserAvatar`；资料卡头像加「✎」角标提示可点；`bindSheet` 改 `$$this.showAvatarSheet`（修「关闭后再点打不开」的双向绑定问题）。
- `pages/Dashboard.ets` / `pages/TaskPlanning.ets`：改用 `UserAvatar`（否则选照片后会显示 `file://…` 原文）。
- 数据层此前已就绪（`User.avatar`、`StorageKey.USER_AVATAR`、`AppStore.saveUserAvatar`），本次只补消费端与选图入口。

### AI 规划页（TaskPlanning.ets 整体重写）
- `common/Theme.ets` 新增对话色板：`BUBBLE_AI`(#F1ECE3 暖米) / `BUBBLE_USER`(#DCEBD1 浅绿) / `CHAT_ACTION`(#7FA65F 橄榄绿) / `AI_AVATAR_BG`(#2E2E2E) / `ONLINE_SOFT` / `ONLINE_DOT`。
- 顶部：左「iKnow」品牌 + 中「AI 任务规划」+「● AI 助手在线」徽标 + 右用户头像（左右固定宽 58 保证真居中）。
- 消息：AI 在左（深色圆「AI」+ 米色气泡），用户在右（浅绿气泡 + 用户头像）；`Scroller` 新消息自动 `scrollEdge(Bottom)`。
- 「待确认任务」居中标题 + 圆圈列表（点圆圈确认变实心，**点文字进编辑**——截图无「编辑」按钮，故去掉可见按钮保持简洁）。
- 底部固定：`✓ 进入 X 分钟专注学习` + 橄榄绿大按钮「确认并开始专注学习」+ 灰字提示。
- 开场白文案对齐截图：`MockAIService.planningIntro()` → 「你好，我是 iKnow，你的专属 AI 学习伙伴。今天准备好来学习什么了吗？」
- 合规取舍：截图头部为「AI 助手在线」徽标（未用 `AITag` 的「AI 智能体」文案）；§11 的「非真人」声明仍由 `AIFirstDisclaimer` 承担。如需严格保留「AI 智能体」字样，需用户确认。

### 踩坑（ArkTS 编译）
- **`@Component` 的 `@Prop` 不能命名为 `size`** —— 与 ArkUI 通用属性 `size()` 冲突，报 `Type 'number' is not assignable to '(value: SizeOptions) => CommonAttribute'`。改名 `diameter` 解决。同类风险名：`width`/`height`/`padding`/`margin`/`visibility` 等通用属性名。
- 自定义组件根节点是 `if/else` 时，不建议直接在组件上用 `.onClick()`；用 `Column(){...}.onClick()` 包裹更稳。

验证：DevEco hvigorw **clean + 增量 assembleHap 均 BUILD SUCCESSFUL，零 ERROR**。剩余 WARN 全为既有风格（`getContext`/`showToast`/`pushUrl` 废弃 + `@Entry export` 预览 + 「function may throw」）。

## 修复：GatewayAIService.ets 编译错误（arkts-no-untyped-obj-literals）
构建报 6 处 ERROR 均在 GatewayAIService.ets:94-100，因 `buildRequestBody` 返回 `Object` 且含无类型对象/数组字面量。
修复：新增显式接口 `ChatMessage` / `GatewayResponseFormat` / `GatewayRequestBody`，并将返回类型改为 `GatewayRequestBody`（提供上下文类型，内层字面量合法）。其余 120 处 WARN（StorageService/页面 pushUrl 等废弃 API）为既有、不阻断构建。
