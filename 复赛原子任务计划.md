# iKnow 复赛原子任务计划

> **赛道**：Agent 创新 ｜ **复赛提交截止**：2026-09-30 24:00（线上评审）
> **版本**：v0.1 ｜ **生成日期**：2026-09-10 ｜ **状态**：路线图，每阶段结束可调整
> **协作前提**：与队友共享 GitHub 仓库；每条任务独立小步、可回滚；改动前先对齐

---

## 0. 怎么用这份文档

- 任务 ID 规则：`S`=可行性验证，`A`=保命修复，`B`=Agent 能力，`C`=鸿蒙特性，`D`=交付物。
- 状态标记：`⬜ 未开始` / `🔄 进行中` / `✅ 完成` / `⛔ 阻塞`。
- 每条任务的「验收」都是**可观测**的；由于当前环境无法构建 HAP，验收统一在 DevEco + 本地模拟器完成。
- 一条任务 = 一个小步。做完即验，验过再进下一条。不要并行推进同一条。

### 得分地图（谁为哪项得分负责）

| 评分项 | 分值 | 主要承载任务 |
|---|---|---|
| 创新（鸿蒙技术创新性/产品创意/交互创新） | **50** | B1–B5、C1–C5 |
| 完备度（功能完备、体验完整顺畅） | 20 | A1–A5、全流程联调 |
| 前景评估（需求真实性、社会价值、商业价值） | 20 | A5、B3/B4、D3 |
| 规范（文档、表达、演示清晰） | 10 | D1–D4 |
| 附加·实际应用（可演示 HAP/源码、上架） | 20 | A1–A5、D4 |

> **最高优先级判断**：`A` 保 40 分（附加 20 + 完备度 20），`C` 冲 50 分创新，`D` 放大全部主观分。`B` 是让 `C` 有灵魂、让「创新」不只是堆特性的关键。

---

## 1. 赛制要点与现状差距

### 1.1 复赛必须提交（规程 3.2）

1. **演示 Demo**：Agent 赛道须提供**源代码文件**（可另附 HAP 加分）
2. **演示视频**：≤ 5 分钟
3. **演示 PPT**

> 复赛为**线上评审**，评委不现场追问 → 视频/PPT 是第一印象，HAP 必须真能装、真能跑通闭环。

### 1.2 关键差距（Gap）

| 维度 | 现状 | 目标 |
|---|---|---|
| 鸿蒙特性 | **0 个** | ≥ 3 个（官网明文建议） |
| Agent 主动性 | 三档硬编码干预 | 干预升级阶梯 + 预算 + 反馈闭环 |
| 演示稳定性 | 存在可复现卡死路径 | 全流程无崩溃、无卡死 |
| 数据真实性 | 无数据也显示饱满画像 | 空态规范 + 真实计算 |
| 交付物 | 仅源代码 | 源代码 + HAP + 视频 + PPT |

---

## 2. 全局约束与风险登记册

| ID | 风险 | 影响 | 应对 |
|---|---|---|---|
| R1 | 实况窗（S1）可能需华为资质/真机 | C1 可能不可做 | 备选：服务卡片 + 意图框架 + 通知，凑齐 3 个特性 |
| R2 | 本地模拟器可能无法开两个实例（S3） | 跨设备无法真通信 | C4 降级为单机内「模拟手表」窗口，走单例 Service 通信 |
| R3 | 语音 Kit 在模拟器可能不可用 | B5 无法验证 | 降级为文字，或改用真机录制演示 |
| R4 | 小艺开放平台有审核周期（S4） | C5 可能来不及 | C5 定位为加分项，不影响主线 |
| R5 | 当前环境无法构建/运行 HAP | 无法自验 | 每条改动附「验证步骤」，由你在 DevEco 执行 |
| R6 | 共享仓库并发改动 | 冲突/覆盖队友工作 | 约定分支策略；每条任务独立提交 |

---

## 3. 阶段 0 · 可行性 spike（0.5 天）

**目的**：这几条决定阶段 3 的技术选型，必须先验，避免押错方向后返工。

### S1 · 实况窗可用性 ⬜

- **验证目的**：API 12 是否提供实况窗能力、是否需要华为侧资质/白名单、模拟器能否显示。
- **操作**：查 HarmonyOS 开发者文档确认对应 Kit 名称与最低 API；在工程内新建最小示例，尝试创建并展示一个实况窗；在本地模拟器运行。
- **通过标准**：本地模拟器能显示实况窗并被更新。
- **失败兜底**：放弃 C1，改用「服务卡片 + 意图框架 + 通知」凑特性数。
- **风险**：最高。实况窗常见限制是需要真机与申请，务必先验。

### S2 · 服务卡片可创建性 ⬜

- **验证目的**：当前工程（API 12 / ArkTS）能否新增 ArkTS 卡片。
- **操作**：在 `entry` 模块新增 FormExtensionAbility + 卡片配置 + 卡片布局；模拟器桌面添加卡片。
- **通过标准**：桌面出现卡片，且能通过数据刷新。
- **风险**：低。

### S3 · 双模拟器实例 ⬜

- **验证目的**：本地能否同时运行两个模拟器实例（决定跨设备能否做**真通信**演示）。
- **操作**：Device Manager 尝试并行启动两个 Phone 实例。
- **通过标准**：两实例同时运行。
- **失败兜底**：C4 改为同一应用内的「模拟手表」页面/悬浮窗，通过单例桥接服务与 `FocusController` 通信。

### S4 · 小艺开放平台 ⬜

- **验证目的**：能否创建测试态 Agent/Skill 并被小艺调起。
- **操作**：注册小艺开放平台，创建最小 Skill，配置一个意图。
- **通过标准**：测试态可被唤起。
- **风险**：审核周期不可控，只作为加分项。

---

## 4. 阶段 1 · 保命（3 天）→ 保 40 分

> 目标：让 HAP 在演示路径上**绝不崩溃/卡死**，并消除「假数据」对真实性的伤害。

### A1 · 修复 RESTING 卡死 ✅（问题已核实）

- **问题定位**：
  - `pages/Focus.ets:61-64` 的 `rest()` 调 `FocusController.startBreak()` 后 `pushUrl('pages/FocusBreak')`。
  - `pages/FocusBreak.ets:25-30` 的 `aboutToDisappear()` **只 clearInterval，不调 `endBreak()`**。
  - `services/FocusController.ets:136-140` 的 `tick()` 在 `resting` 时 `breakSec++` 后直接 `return`。
  - 结果：用户在休息页按**系统返回键**（而非「继续专注」按钮）→ `resting` 仍为 true、`state` 停在 RESTING、摄像头已停止 → **总时长/净专注永久冻结**；且 `running` 仍为 true，`Focus.ets:28` 的 `if (!isRunning())` 不会自愈。
- **改动方案**（建议合并为「休息态由 controller 独占管理」）：
  1. `FocusBreak` 增加 `onBackPress` 处理：先 `endBreak()` 再 `router.back()`。
  2. `aboutToDisappear()` 增加兜底：若 controller 仍在 resting，调用 `endBreak()`。
  3. `FocusBreak` 页面的倒计时不再自起 `setInterval`，改为由 controller 提供剩余秒数（避免两套计时不同源）。
  4. 「再休息 5 分钟」改为通知 controller 重置休息目标时长，而非只重置页面 `@State`。
- **验收**：休息页按系统返回 → 回专注页计时继续增长；连续多次进出休息、暂停、答疑后不出现停摆。
- **估时**：0.5–1 天

### A2 · 计时口径改为墙钟驱动 ⬜

- **问题定位**：`services/FocusController.ets:129-156` 每 1000ms `totalSec++`，速记于 tick 次数。后台/息屏/系统挂起会漏 tick → 时长偏短、`focusRate` 失真。
- **改动方案**：
  1. 引入「段（segment）」模型：记录每段 `{state, startTs}`；状态切换/暂停/答疑/休息时用 `Date.now() - startTs` **结算**上一段。
  2. `tick` 仅负责 UI 刷新（`pushLive`）与结算当前段，计时不再依赖 tick 次数。
  3. （可选）后台长时任务 + 周期快照，实现进程被杀后的会话恢复。
- **验收**：专注中息屏 60s 后返回，`elapsed` 增量 ≈ 60s（误差 < 2s）；正常前台使用时与旧行为一致。
- **估时**：1–1.5 天
- **注意**：A1 与 A2 都在 `FocusController` 计时模型上，建议**先定模型再动手**，A1 的修复可直接建立在 A2 的段模型上，避免改两遍。

### A3 · AI 人格真正生效 + 启动恢复 ⬜

- **问题定位**：
  - `services/AIService.ets` 的 `chat / answerQuestion / reviewInsight / reviewSuggestions / proactive / planTasks` **全部返回固定文案**，无任何人格分支。
  - `store/AppStore.ets:50-70` 的 `doInit()` 加载 user 后**未调用** `AIService.setPersonality(user.aiPersonality)`（仅 `saveUserPersonality` 里调）。
- **改动方案**：
  1. `doInit()` 在 user 加载/seed 后补 `AIService.getInstance().setPersonality(this.user.aiPersonality)`。
  2. 建立**人格差异表**，覆盖四型（STRICT / GENTLE / QUIET / ADAPTIVE）在开场白、建议密度、语气上的差异：
     - STRICT：直接、聚焦当下这一小步，不打击、不道德绑架
     - GENTLE：鼓励、低压力
     - QUIET：**真的少打扰** —— 不仅换措辞，而是关闭/提高主动提醒阈值（与 B1 干预预算联动）
     - ADAPTIVE：依据长期数据自适应；MVP 可先退化为 GENTLE + 少量可调参数，并诚实标注
- **验收**：切换四种人格，同一问题的文案与主动建议频率有**可观察差异**；杀进程重启后保持所选人格。
- **估时**：1 天
- **依赖**：B1（QUIET 的「少打扰」需干预预算支持）

### A4 · 隐私授权变成真实门禁 ⬜

- **问题定位**：`pages/Privacy.ets:9-12` 的 `allowCamera/allowMic/allowHealth` 是组件 `@State`，不落盘、不被任何 Service 读取；关闭后 `Focus.ets` 仍固定显示「摄像头感知中」并启动检测。这与「分项授权」的合规承诺直接冲突。
- **改动方案**：
  1. 授权状态持久化（扩展 `user` 表列或新增 `consent` 表；在 `StorageService` 增加读写）。
  2. `FocusDetectionService.start()` 增加**前置门禁**：未授权摄像头则不启动。
  3. `FocusController.start()` 依授权决定是否订阅检测；未授权时进入**降级模式**（仅计时、无状态感知）。
  4. `Focus.ets` 顶部依据真实授权显示：未授权时改为「本次未开启感知」，并提供一键授权入口。
  5. 健康数据授权同样作 `SensorService` 读取的前置条件。
- **验收**：关闭「允许摄像头」→ 专注页不再显示「摄像头感知中」，无状态感知但计时正常；重启后开关状态保持；清理数据不影响授权记录（或按产品定义明确处理）。
- **估时**：1 天
- **得分**：合规与真实性，直接支撑「前景评估」。

### A5 · 空态与假数据清理 ⬜

- **问题定位**：
  - `models/FocusProfile.ets:3-16` 默认值 `efficiency=82/.../antiDistraction=72`。
  - `store/AppStore.ets:190-204` 的 `genMonthlyTrend()` 随机游走，与会话无关。
  - `store/AppStore.ets:113-127`（seed）与 `437-473`（clearAll）都把 `bestFocusTime` 写死为 `'09:30 - 11:30'`。
- **改动方案**：
  1. 删除 `genMonthlyTrend()` 的随机游走；`monthlyTrend` 改由真实会话计算，样本不足则留空。
  2. `bestFocusTime` 由历史会话的专注表现按时段聚合得出；样本不足则为空。
  3. `FocusProfile.ets` 页面：会话数 < 阈值（建议 3）→ 显示**「还在了解你」空态**，不渲染雷达数值。
  4. `clearAll` 后的 profile 回到「干净默认」（数值语义为「无数据」而非伪造高分）。
- **验收**：清除全部数据 → 画像页显示空态；积累 ≥3 次真实会话后逐步出现真实数值与趋势。
- **估时**：1 天
- **得分**：直接支撑「前景评估 20 分」中的**需求真实性**。

---

## 5. 阶段 2 · Agent 感（5 天）→ 创新分主引擎

> Agent 赛道三要素：**主动服务 / 陪伴 / 自然交互**。本阶段把「主动服务」从口号变成可验证的机制。

### B1 · 干预升级阶梯 + 预算 + 反馈闭环 ⬜ ⭐核心

- **问题定位**：`FocusController.onDetection()` 三档硬编码；红灯只弹一次固定询问，无升级、无预算、无反馈回收。「智能适配」与「抗干扰」五维因此没有真实数据来源。
- **改动方案**：
  1. 定义 **escalation ladder**（示例，阈值可调）：

     | 级别 | 行为 | 触发（连续分心时长） |
     |---|---|---|
     | L0 | 不打扰 | — |
     | L1 | 采样加密（无感） | > 10s |
     | L2 | 呼吸灯节奏变化（无文案） | > 30s |
     | L3 | 边缘柔和光晕（无文案） | > 60s |
     | L4 | 一句轻提示 | > 90s |
     | L5 | 主动询问（继续/休息/结束） | > 150s |
     | L6 | 建议休息并自动结算 | > 240s |

  2. **干预预算**：每 30 分钟 `L4+` 合计 ≤ N 次（建议 2）。超预算则降级为无感反馈。
  3. **反馈回收**：记录用户对 L4/L5 的反应（继续/休息/结束/忽略）并写回策略参数（下一轮阈值/密度微调）。
  4. **人格联动**：QUIET 关闭 L4/L5 或大幅提高阈值；STRICT 可适当降低阈值但不得指责。
- **触点**：`FocusController`（阈值/预算/升级 + 新事件）、`AIService`（L4 文案按人格）、`pages/Focus.ets`（各档 UI）、`models`（新增干预事件或扩展 `FocusEvent`）、`StorageService`（落库）。
- **验收**：注入持续分心 → 干预按阈值逐级递进；30 分钟内 L4+ 不超过设定次数；QUIET 人格下不出现 L4/L5；干预反馈被记录。
- **估时**：2 天
- **得分**：创新（主动服务）+ 交互创新。**这是视频/答辩的核心卖点。**

### B2 · 状态机去抖 ⬜

- **问题定位**：`FocusController.onDetection()`（159-171）直接透传检测 `target`，无持续时间判定；`confidence` 落了库但从未被读；`stateSinceSec` 自增但未被消费。真实摄像头必然抖动，会导致绿↔黄↔红高频闪烁。
- **改动方案**：引入去抖——目标状态需**连续 N 次或持续 T 秒**才提交；升级与降级使用不同阈值；`confidence` 低于阈值忽略；`DISTRACTED` 采用更长确认时间（误报比漏报伤害更大）。
- **验收**：脚本注入 2–3s 的短暂抖动不触发切换；持续偏离超过 T 才切换。
- **估时**：0.5 天
- **依赖**：B1（递进逻辑需要稳定状态）

### B3 · 任务级归因 ⬜

- **问题定位**：
  - `models/Task.ets` 的 `actualDuration` 有字段、DB 有列，**从未被赋值**（恒为 0）。
  - `AppStore.endSession()`（303-332）保存任务时 `Task.sessionId` 仍为空串。
  - `FocusSession` 只有全局 timeline，没有「第几分钟在哪个任务」。
  - `FocusProfile.hardestSubject()` 用「完成率最低」近似，语义是「进度最慢」而非「最易分心」。
- **改动方案**：
  1. `FocusController` 记录 **taskTimeline**（任务切换点 + 每任务累计时长），或给 `FocusTimelinePoint` 增加 `taskId`。
  2. 结束会话时把 `actualDuration`、`sessionId` 写回 `Task`。
  3. `ReportService` 增加任务级归因：最易分心任务（按分心率而非完成率）、耗时偏差（实际 vs 预估）。
  4. `FocusProfile.hardestSubject()` 改为基于分心数据。
- **验收**：复盘能输出「XX 任务在第 N 分钟后开始分心」类洞察；DB 中 `task.actual_duration` / `session_id` 非空。
- **估时**：1.5 天
- **依赖**：A2（段结算模型）

### B4 · 五维口径重算 ⬜

- **问题定位**：`AIService.updateProfile()`（163-187）中 `concentration/efficiency/antiDistraction` 同源派生；`stability = 60 + avgFocusMin*0.4` 是时长的线性函数而非稳定性；`execution = 78 + count*1.5` 会迅速打满，失去区分度。
- **改动方案**：定义五维口径表并分别实现：

  | 维度 | 业务含义 | 计算依据 |
  |---|---|---|
  | 效率 | 单位时间产出 | 任务完成量 / 预估达成率 |
  | 专注度 | 净专注水平 | 净专注率（加权） |
  | 稳定性 | 表现波动程度 | 多次会话净专注率的**标准差反向映射** |
  | 执行力 | 计划落地程度 | 计划任务完成率（按样本量归一） |
  | 抗干扰 | 被扰动后恢复力 | 干预后回到专注的比例 / 分心事件密度 |

  每条需定义：最小样本量、取值映射、更新时机。样本不足 → 显示「数据不足」而非猜数。
- **验收**：五维数值不再高度同源；样本不足时明确提示。
- **估时**：1 天
- **依赖**：B3（需要任务级与干预反馈数据）

### B5 · 语音提问 ⬜

- **问题定位**：`pages/AIChat.ets` 仅文字；PRD 页面 7 承诺「文字 / 语音 / 图片拍照搜题」。
- **改动方案**：接入系统语音识别能力，`AIChat` 增加语音按钮（按住说话 → 转文字 → 提问）。拍照搜题（OCR + 视觉）成本高，**建议放到最后或不做**。
- **验收**：可语音输入并转文字提问，答案正常返回。
- **估时**：1 天
- **风险**：见 R3，语音 Kit 可能在模拟器不可用 → 需先小验，失败则降级为文字并改用真机录制。

---

## 6. 阶段 3 · 鸿蒙特性 ×3（5 天）→ 冲创新 50

> 官网明文「建议集成 3 个及以上鸿蒙特性」。选择**可验证**的三个，避免不可验证的分布式。

### C1 · 实况窗 ⬜（依赖 S1）

- **场景**：专注进行中，状态栏/锁屏实时显示「专注中 · 42:36 · 🟢 状态良好」；干预触发时状态随之变化。
- **为什么选它**：与产品「陪伴感」定位天然契合，鸿蒙辨识度最高，且单机可演示。
- **验收**：专注中退桌面/锁屏可见实况窗；结束专注后消失。
- **估时**：1–1.5 天
- **兜底**：S1 不通过则跳过，用意图框架/通知替代。

### C2 · 服务卡片 ⬜

- **内容**：今日专注时长 / 净专注率 / **AI 此刻建议**（呼应主动服务）；点击跳转规划或专注。
- **验收**：模拟器桌面可添加卡片；数据随专注更新；点击可跳转。
- **估时**：1 天

### C3 · 一多 / 响应式适配 ⬜

- **方案**：以断点/媒体查询适配 phone / foldable / tablet；对首页、工作台、专注、画像做栅格与宽度约束。
- **交付价值**：用 DevEco Previewer 输出多形态截图/录屏，**无需真机**即可呈现「多端适配」。
- **验收**：Previewer 在各形态下不破版。
- **估时**：1.5 天

### C4 · 模拟手表通道 ⬜

- **方案**：新增「模拟设备」页面或悬浮窗作为手表表盘；通过单例桥接服务与 `FocusController` 通信——接收干预事件后展示「震动」动效 + 心率/HRV 示意；可反向触发「我回来了」。
- **硬性要求**：UI 必须标注「**模拟设备**」，不伪装成真实分布式。
- **验收**：专注页触发干预 → 模拟手表同步出现震动/视觉反馈；反向操作能被专注页接收。
- **估时**：1.5 天
- **得分**：多设备联动叙事 + 交互创新，视频亮点。
- **依赖**：S3（若可开双实例，则升级为真通信演示）

### C5 · 小艺 Skill ⬜（条件项，依赖 S4）

- **方案**：把「帮我规划今天学习」「开始专注」「我今天状态不好」做成意图，可被小艺调起并进入对应流程。
- **验收**：小艺测试态可唤起并进入流程。
- **估时**：1–2 天（不含审核）
- **定位**：Agent 赛道高性价比加分项，不影响主线。

---

## 7. 阶段 4 · 交付物（5 天）→ 规范 10 + 放大全部主观分

### D1 · 演示视频脚本（≤5 min）⬜

- **分镜**：痛点（学了却不知道状态）→ AI 规划 → 专注 → 感知与分层干预 → 答疑 → 复盘 → 长期画像 → **鸿蒙特性集中展示**。
- **要求**：写成分镜表（镜号/画面/旁白/时长），总时长控制在 4 分 40 秒内，留余量。

### D2 · 录制 + 剪辑 ⬜

- **要点**：用同一镜头讲「分心 → 手表震动 / 实况窗提示」的跨端叙事；证据优先于口号。

### D3 · 演示 PPT ⬜

- **结构**：对齐评分四项（创新 / 完备 / 前景 / 规范）；含架构图、鸿蒙特性清单、创新点、数据口径、前景与落地。

### D4 · 打包与提交 ⬜

- 源代码 zip +（加分）可运行 HAP；命名 `03-作品名称+队伍名称.zip`。
- 更新 README 的运行说明，确保评委可复现。

---

## 8. 排期（20 天）

| 天 | 任务 | 里程碑 |
|---|---|---|
| D1 | S1–S4 + A1 | spike 结论 + 卡死修复 |
| D2–D3 | A2、A3 | 计时模型更新、人格生效 |
| D4 | A4、A5 | 授权门禁、空态 |
| D5–D6 | B1、B2 | 干预阶梯（核心） |
| D7–D8 | B3、B4 | 任务归因、五维 |
| D9 | B5 | 语音（可降级） |
| D10–D11 | C1、C2 | 实况窗、服务卡片 |
| D12–D13 | C3、C4 | 多形态、模拟手表 |
| D14 | C5 / 缓冲 | 小艺（可选） |
| D15–D16 | D1、D2 | 脚本 + 录制 |
| D17–D18 | D3、D4 | PPT + 打包 |
| D19–D20 | 全流程联调 + 缓冲 | 提交 |

> 排期含约 15% 缓冲；`B5`、`C5` 为可裁剪项，时间不足时优先保证 A / B1 / C1–C4 / D。

---

## 9. 附：改动清单速查

| 任务 | 主要触点文件 |
|---|---|
| A1 | `pages/FocusBreak.ets`、`pages/Focus.ets`、`services/FocusController.ets` |
| A2 | `services/FocusController.ets`、`models/FocusSession.ets` |
| A3 | `services/AIService.ets`、`store/AppStore.ets` |
| A4 | `pages/Privacy.ets`、`pages/Focus.ets`、`services/FocusDetectionService.ets`、`services/FocusController.ets`、`services/StorageService.ets` |
| A5 | `models/FocusProfile.ets`、`store/AppStore.ets`、`pages/FocusProfile.ets` |
| B1 | `services/FocusController.ets`、`services/AIService.ets`、`pages/Focus.ets`、`models/`、`services/StorageService.ets` |
| B2 | `services/FocusController.ets` |
| B3 | `services/FocusController.ets`、`store/AppStore.ets`、`services/ReportService.ets`、`models/Task.ets` |
| B4 | `services/AIService.ets`、`models/FocusProfile.ets`、`pages/FocusProfile.ets` |
| B5 | `pages/AIChat.ets` |
| C1–C5 | 新增模块/页面 + 工程配置（`module.json5`、`build-profile.json5` 等） |
| D1–D4 | `README.md` + 交付物（仓库外） |

---

*本文件为计划文档，未修改任何源码。*
